//mass in kg
var markMass = 80;
//height in meters
var markHeight = 1.80;
//mass in kg
var JohnMass = 75;
//height in meters
var johnHeight = 1.75;
// caluculating BMI
var markBMI = markMass / (markHeight * markHeight);
var johnBMI = JohnMass / (johnHeight * johnHeight);
// statement to determine which BMI is higher
var markhiegherBM1 = markBMI > johnBMI;
console.log(markhiegherBM1);

